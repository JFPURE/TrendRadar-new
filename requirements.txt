# requests: 用于发送 HTTP/HTTPS 请求，常用于访问 Web API。
requests>=2.32.5,<3.0.0

# pytz: 提供时区转换和操作，适用于日期与时间处理。
pytz>=2025.2,<2026.0

# PyYAML: 用于解析和生成 YAML 格式的数据，经常用于配置文件读写。
PyYAML>=6.0.3,<7.0.0

# fastmcp: 快速的多渠道发布（MCP）相关库；用于高效管理多平台信息流。
fastmcp>=2.12.0,<2.14.0

# websockets: 支持异步 WebSocket 通信，用于实时数据传输。
websockets>=13.0,<14.0

# boto3: Amazon Web Services (AWS) 的官方 Python SDK，常用于操作 S3、EC2 等云服务资源。
boto3>=1.35.0,<2.0.0

# feedparser: 用于解析 RSS、Atom 等内容订阅源，支持多种新闻聚合格式。
feedparser>=6.0.0,<7.0.0
litellm>=1.57.0,<2.0.0
tenacity==8.5.0
