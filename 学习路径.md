# TrendRadar 项目学习路径

## 📚 学习目标

掌握一个完整的热点新闻聚合与分析系统的设计、实现和部署。

---

## 🎯 第一阶段：项目概览与快速上手（1-2 天）

### 1.1 理解项目定位

- **阅读文档**：`README.md` 的核心功能部分
- **了解项目**：热点新闻聚合、关键词筛选、多渠道推送
- **查看演示**：访问 [GitHub Pages 演示](https://sansan0.github.io/TrendRadar/)

**学习要点**：

- 项目解决了什么问题？
- 目标用户群体是谁？
- 核心价值是什么？

### 1.2 快速部署体验

- **方式一**：GitHub Actions 部署（最简单）

  1. Fork 项目到自己的 GitHub
  2. 配置 GitHub Secrets（至少配置一个推送渠道）
  3. 手动触发一次 Actions 运行
  4. 观察推送效果

- **方式二**：Docker 本地部署
  ```bash
  git clone https://github.com/sansan0/TrendRadar.git
  cd TrendRadar
  # 按照 README 中的 Docker 部署步骤操作
  ```

**学习要点**：

- 理解项目的部署方式
- 体验完整的工作流程
- 理解配置文件的作用

### 1.3 配置文件理解

**重点文件**：

- `config/config.yaml` - 主配置文件
- `config/frequency_words.txt` - 关键词配置

**学习任务**：

1. 修改 `frequency_words.txt`，添加自己关心的关键词
2. 理解三种推送模式的区别（daily/current/incremental）
3. 配置一个推送渠道（企业微信/飞书/Telegram）

---

## 🔍 第二阶段：核心架构理解（3-5 天）

### 2.1 项目结构分析

```
trendradar/
├── __main__.py          # 程序入口
├── context.py           # 应用上下文（核心抽象）
├── core/               # 核心功能模块
│   ├── config.py       # 配置管理
│   ├── loader.py       # 配置加载
│   ├── frequency.py    # 关键词匹配
│   ├── data.py         # 数据读写
│   └── analyzer.py     # 统计分析
├── crawler/            # 爬虫模块
│   └── fetcher.py      # 数据获取
├── storage/            # 存储模块
│   ├── base.py         # 存储基类
│   ├── local.py        # 本地SQLite存储
│   ├── remote.py       # 远程云存储
│   └── manager.py      # 存储管理器
├── notification/       # 通知模块
│   ├── dispatcher.py   # 通知调度器
│   ├── senders.py      # 各平台发送器
│   └── formatters.py   # 消息格式化
├── report/             # 报告生成
│   ├── generator.py    # HTML生成
│   └── formatter.py    # 数据格式化
└── utils/              # 工具函数
    └── time.py         # 时间处理
```

**学习任务**：

1. 画出模块依赖关系图
2. 理解每个模块的职责
3. 找出模块间的调用关系

### 2.2 数据流分析

**核心流程**：

```
数据爬取 → 数据存储 → 关键词匹配 → 统计分析 → 报告生成 → 通知推送
```

**学习任务**：

1. 追踪一次完整的数据流（从爬取到推送）
2. 理解 `NewsAnalyzer` 类的 `run()` 方法
3. 理解 `AppContext` 的作用（上下文模式）

**关键代码阅读**：

- `trendradar/__main__.py` - `NewsAnalyzer.run()`
- `trendradar/context.py` - `AppContext` 类

### 2.3 存储架构理解

**存储后端**：

- 本地 SQLite（Docker/本地环境）
- 远程云存储（GitHub Actions，S3 兼容）

**学习任务**：

1. 阅读 `trendradar/storage/base.py` 理解存储抽象
2. 理解 `trendradar/storage/local.py` 的 SQLite 实现
3. 理解 `trendradar/storage/manager.py` 的存储管理器模式

**关键概念**：

- 存储后端自动选择（auto 模式）
- 数据格式（SQLite/TXT/HTML）
- 数据保留策略

---

## 💻 第三阶段：核心功能深入（5-7 天）

### 3.1 爬虫模块（crawler）

**学习重点**：

- `trendradar/crawler/fetcher.py` - `DataFetcher` 类

**学习任务**：

1. 理解 API 调用逻辑
2. 理解重试机制
3. 理解代理支持
4. 理解请求间隔控制

**实践任务**：

- 尝试添加一个新的平台（参考 `config.yaml` 中的平台配置）
- 理解数据格式转换（`convert_crawl_results_to_news_data`）

### 3.2 关键词匹配（core/frequency）

**学习重点**：

- `trendradar/core/frequency.py` - `load_frequency_words()` 和 `matches_word_groups()`

**学习任务**：

1. 理解五种语法：
   - 普通词
   - 必须词（`+`）
   - 过滤词（`!`）
   - 数量限制（`@`）
   - 全局过滤（`[GLOBAL_FILTER]`）
2. 理解词组功能（空行分隔）
3. 理解匹配优先级

**实践任务**：

- 编写测试用例验证各种匹配规则
- 理解 `matches_word_groups()` 的实现逻辑

### 3.3 统计分析（core/analyzer）

**学习重点**：

- `trendradar/core/analyzer.py` - `count_word_frequency()` 和 `calculate_news_weight()`

**学习任务**：

1. 理解热点权重算法：
   - 排名权重（60%）
   - 频次权重（30%）
   - 热度权重（10%）
2. 理解三种推送模式的数据处理差异
3. 理解新增新闻检测逻辑

**实践任务**：

- 修改权重配置，观察排序变化
- 理解 `count_word_frequency()` 的统计逻辑

### 3.4 报告生成（report）

**学习重点**：

- `trendradar/report/generator.py` - HTML 报告生成
- `trendradar/report/formatter.py` - 数据格式化

**学习任务**：

1. 理解 HTML 模板结构
2. 理解报告数据的组织方式
3. 理解不同模式下的报告差异

**实践任务**：

- 修改 HTML 模板，自定义样式
- 理解 `generate_html_report()` 的生成流程

### 3.5 通知推送（notification）

**学习重点**：

- `trendradar/notification/dispatcher.py` - `NotificationDispatcher` 类
- `trendradar/notification/senders.py` - 各平台发送器

**学习任务**：

1. 理解多账号推送机制
2. 理解消息分批逻辑（`splitter.py`）
3. 理解各平台的消息格式差异
4. 理解推送时间窗口控制

**实践任务**：

- 添加一个新的推送渠道（参考现有实现）
- 理解 `dispatch_all()` 的调度逻辑

---

## 🗄️ 第四阶段：存储系统深入（3-4 天）

### 4.1 存储抽象层

**学习重点**：

- `trendradar/storage/base.py` - `StorageBackend` 基类

**学习任务**：

1. 理解存储接口设计
2. 理解数据模型（`NewsData`）
3. 理解存储操作的抽象

### 4.2 本地存储实现

**学习重点**：

- `trendradar/storage/local.py` - SQLite 存储实现
- `trendradar/storage/schema.sql` - 数据库结构

**学习任务**：

1. 理解数据库表结构设计
2. 理解数据查询逻辑
3. 理解数据清理机制

**实践任务**：

- 使用 SQLite 工具查看数据库内容
- 理解 `save_news_data()` 和 `query_news()` 的实现

### 4.3 远程存储实现

**学习重点**：

- `trendradar/storage/remote.py` - S3 兼容存储实现

**学习任务**：

1. 理解 S3 兼容协议
2. 理解数据上传/下载逻辑
3. 理解存储路径设计

**实践任务**：

- 配置 Cloudflare R2（免费）
- 理解远程存储的数据同步

### 4.4 存储管理器

**学习重点**：

- `trendradar/storage/manager.py` - `StorageManager` 类

**学习任务**：

1. 理解存储后端选择逻辑
2. 理解数据格式管理（SQLite/TXT/HTML）
3. 理解数据拉取机制（pull 功能）

---

## 🤖 第五阶段：AI 分析功能（MCP）（4-5 天）

### 5.1 MCP 协议理解

**学习重点**：

- `mcp_server/server.py` - MCP 服务器实现

**学习任务**：

1. 理解 Model Context Protocol（MCP）协议
2. 理解 MCP 工具的定义和使用
3. 理解 STDIO 和 HTTP 两种传输模式

**参考资料**：

- [MCP 官方文档](https://modelcontextprotocol.io/)
- `README-MCP-FAQ.md` - 项目 MCP 使用文档

### 5.2 MCP 工具实现

**学习重点**：

- `mcp_server/tools/` - 各种分析工具

**13 种工具分类**：

1. **基础查询**：`data_query.py`

   - `get_latest_news` - 获取最新新闻
   - `get_news_by_date` - 按日期查询
   - `get_trending_topics` - 获取热门话题

2. **智能检索**：`search_tools.py`

   - `search_news` - 搜索新闻
   - `search_related_news_history` - 历史关联检索

3. **高级分析**：`analytics.py`

   - `analyze_topic_trend` - 话题趋势分析
   - `analyze_data_insights` - 数据洞察
   - `analyze_sentiment` - 情感分析
   - `find_similar_news` - 相似新闻查找
   - `generate_summary_report` - 生成摘要报告

4. **系统管理**：`system.py`、`config_mgmt.py`
   - `get_current_config` - 获取配置
   - `get_system_status` - 系统状态
   - `trigger_crawl` - 触发爬取

**学习任务**：

1. 理解每个工具的实现逻辑
2. 理解工具的参数和返回值
3. 理解工具如何与存储系统交互

### 5.3 MCP 客户端配置

**学习任务**：

1. 配置 Claude Desktop（STDIO 模式）
2. 配置 Cursor（HTTP 模式）
3. 配置 Cherry Studio（GUI 配置）

**实践任务**：

- 使用 AI 客户端查询新闻数据
- 理解自然语言到工具调用的转换

---

## 🏗️ 第六阶段：架构设计模式（3-4 天）

### 6.1 设计模式应用

**学习重点**：

1. **上下文模式（Context Pattern）**

   - `trendradar/context.py` - `AppContext` 类
   - 消除全局状态，封装配置依赖

2. **策略模式（Strategy Pattern）**

   - `trendradar/__main__.py` - `MODE_STRATEGIES`
   - 三种推送模式的策略实现

3. **工厂模式（Factory Pattern）**

   - `trendradar/storage/manager.py` - `get_storage_manager()`
   - 存储后端的创建

4. **模板方法模式（Template Method）**
   - `trendradar/__main__.py` - `_run_analysis_pipeline()`
   - 统一的分析流水线

**学习任务**：

1. 识别代码中的设计模式
2. 理解为什么使用这些模式
3. 思考如何改进设计

### 6.2 模块化设计

**学习重点**：

- 模块职责划分
- 模块间依赖关系
- 接口设计原则

**学习任务**：

1. 分析模块间的耦合度
2. 理解如何降低耦合
3. 理解如何提高可测试性

---

## 🧪 第七阶段：实践与扩展（5-7 天）

### 7.1 功能扩展

**实践项目**：

1. **添加新平台**

   - 研究 NewsNow API 支持的平台
   - 在 `config.yaml` 中添加配置
   - 测试数据获取

2. **添加新推送渠道**

   - 参考现有实现（如 `notification/senders.py`）
   - 实现新的发送器类
   - 在 `dispatcher.py` 中注册

3. **自定义报告格式**

   - 修改 HTML 模板
   - 添加新的数据可视化
   - 自定义样式

4. **优化关键词匹配**
   - 添加新的匹配规则
   - 优化匹配性能
   - 添加匹配测试

### 7.2 性能优化

**优化方向**：

1. 数据库查询优化
2. 内存使用优化
3. 网络请求优化
4. 并发处理优化

**实践任务**：

- 使用性能分析工具（如 `cProfile`）
- 找出性能瓶颈
- 实施优化方案

### 7.3 测试编写

**测试类型**：

1. 单元测试（各模块功能）
2. 集成测试（模块间协作）
3. 端到端测试（完整流程）

**实践任务**：

- 为关键模块编写测试用例
- 使用 pytest 框架
- 实现 CI/CD 测试流程

---

## 📖 第八阶段：部署与运维（2-3 天）

### 8.1 部署方式理解

**部署方式**：

1. **GitHub Actions**（云端自动化）
2. **Docker**（容器化部署）
3. **本地运行**（开发调试）

**学习任务**：

1. 理解 GitHub Actions 工作流
2. 理解 Docker 镜像构建
3. 理解环境变量配置

### 8.2 监控与日志

**学习重点**：

- 日志记录机制
- 错误处理机制
- 数据清理策略

**实践任务**：

- 配置日志系统
- 实现错误告警
- 监控数据存储使用

---

## 🎓 学习建议

### 学习方法

1. **先运行，再理解**：先让项目跑起来，再深入代码
2. **边学边做**：每学一个模块，就尝试修改或扩展
3. **画图理解**：用流程图、架构图帮助理解
4. **阅读测试**：如果有测试代码，先看测试理解功能

### 重点难点

1. **存储架构**：理解多存储后端的抽象和实现
2. **关键词匹配**：理解复杂的匹配规则和优先级
3. **MCP 协议**：理解 AI 工具的定义和调用
4. **上下文模式**：理解 AppContext 的设计思想

### 推荐阅读顺序

1. `README.md` - 了解项目全貌
2. `trendradar/__main__.py` - 理解主流程
3. `trendradar/context.py` - 理解上下文设计
4. `trendradar/core/` - 理解核心功能
5. `trendradar/storage/` - 理解存储系统
6. `trendradar/notification/` - 理解通知系统
7. `mcp_server/` - 理解 AI 分析功能

### 进阶方向

1. **性能优化**：数据库优化、并发处理
2. **功能扩展**：新平台、新推送渠道、新分析工具
3. **架构改进**：微服务化、分布式部署
4. **AI 增强**：更智能的分析算法、预测功能

---

## 📚 参考资源

### 项目文档

- `README.md` - 主文档
- `README-EN.md` - 英文文档
- `README-MCP-FAQ.md` - MCP 使用文档
- `README-Cherry-Studio.md` - Cherry Studio 配置文档

### 技术文档

- [MCP 协议文档](https://modelcontextprotocol.io/)
- [NewsNow API](https://github.com/ourongxing/newsnow)
- [SQLite 文档](https://www.sqlite.org/docs.html)
- [S3 兼容协议](https://docs.aws.amazon.com/AmazonS3/latest/API/Welcome.html)

### 代码阅读工具

- IDE：VS Code / PyCharm
- 调试：Python Debugger
- 性能分析：cProfile, line_profiler
- 架构图：PlantUML, Mermaid

---

## ✅ 学习检查清单

### 基础理解

- [ ] 理解项目的核心功能和价值
- [ ] 能够成功部署项目（至少一种方式）
- [ ] 理解配置文件的作用和修改方法
- [ ] 能够配置关键词和推送渠道

### 架构理解

- [ ] 理解项目的模块划分和职责
- [ ] 理解数据流的完整路径
- [ ] 理解存储架构的设计
- [ ] 理解上下文模式的应用

### 功能实现

- [ ] 理解爬虫模块的实现
- [ ] 理解关键词匹配逻辑
- [ ] 理解统计分析算法
- [ ] 理解报告生成流程
- [ ] 理解通知推送机制

### 高级功能

- [ ] 理解 MCP 协议和实现
- [ ] 能够配置 MCP 客户端
- [ ] 理解存储后端的抽象和实现
- [ ] 理解设计模式的应用

### 实践能力

- [ ] 能够添加新平台
- [ ] 能够添加新推送渠道
- [ ] 能够修改报告格式
- [ ] 能够编写测试用例
- [ ] 能够进行性能优化

---

**祝你学习愉快！** 🎉

如有问题，可以：

- 查看项目 Issues
- 阅读项目文档
- 参考代码注释
- 实践调试理解
